$(document).ready(function () {
    //Assign a variable called paragraphs equal to all the p tags in the documentElement
    var paragraphs = $('p');
    $(paragraphs).css({'color': 'maroon', 'background-color': 'lavender', 'line-height': '1.5rem'});
});
